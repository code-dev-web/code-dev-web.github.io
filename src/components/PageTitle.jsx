export const PageTitle = ({ title }) => (
  <h2 className="primary-font">{title}</h2>
);

export const SubTitle = ({ title }) => (
  <h3 className="secondary-font">{title}</h3>
);

export const Text = ({ text }) => <h4 className="secondary-font">{text}</h4>;
